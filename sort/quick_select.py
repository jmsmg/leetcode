def quick_select(array:list, rank:int) -> int:
    if len(array) < 2:
        return -1
    
    idx_l = 0
    idx_r = len(array) - 2
    mid = idx_r // 2
    pivot = len(array) - 1
    
    while 
    
array = list(map(int, input().split()))
rank = int(input())

print(quick_select(array, rank))